{% set name = "pycbf" %}
{% set version = "0.9.6.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: ddfb64b9b7bf9f7e8be15667a09ac6e0daa3d4215e4aa29aec6eba02990624f9

build:
  skip: true  # [win]
  number: 0
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  build:
    - python                                 # [build_platform != target_platform]
    - cross-python_{{ target_platform }}     # [build_platform != target_platform]
    - cython                                 # [build_platform != target_platform]
    - {{ compiler('c') }}
  host:
    - cython
    - python
    - pip
    - poetry-core
  run:
    - python
    - numpy >=1.17

test:
  imports:
    - pycbf
  requires:
    - pip
  commands:
    - pip check

about:
  home: https://github.com/dials/pycbf
  license: LGPL-2.1-or-later
  license_family: LGPL
  license_file: LICENCE.txt
  summary: An API for CBF/imgCIF Crystallographic Binary Files
  description: |
    pycbf is a Python library for reading CBF formatted files.
  dev_url: https://github.com/dials/pycbf

extra:
  recipe-maintainers:
    - rjgildea
    - ndevenish
    - anthchirp
